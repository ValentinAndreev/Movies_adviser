= form_with model: [@commentable, @comment] do |form|
  = form.label :comment
  = form.text_field :body, as: :text, required: true, class: 'form-control'
  = form.button :Submit, class: "btn btn-link"