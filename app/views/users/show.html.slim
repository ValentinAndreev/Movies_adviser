| Name: #{@user.username} #{image_tag(@user.avatar.url, height: '40', class: "rounded-circle")}
br/
| Registration: #{@user.created_at}
br/
| About me: #{@user.about}
br/
= link_to_if(@user.following_count > 0, "following #{@user.following_count}", following_user_path(@user))
br/
= link_to_if(@user.followers_count > 0, "followers #{@user.followers_count}", followers_user_path(@user))
br/
= render "#{@user.action(current_user)}"